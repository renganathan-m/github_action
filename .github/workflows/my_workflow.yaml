
name: Toy Workflow
run-name: Toy Worflow Exeuted!

on:
    - push

jobs:
       print_job-1:
            runs-on:   ubuntu-latest
            steps:
               - name: First Print
                 run: |
                   echo "Hello! I am printing <-- from print job-1"
                  
